<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

        <item android:state_checked="true">
            <color android:color="@android:color/transparent" /> <!-- Transparent color when item is checked -->
        </item>
        <item>
            <color android:color="@android:color/transparent" /> <!-- Transparent color for other states -->
        </item>

</selector>